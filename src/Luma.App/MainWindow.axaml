<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:c="using:Luma.App.Controls"
        x:Class="Luma.App.MainWindow"
        Width="52" Height="52" MinWidth="52" MinHeight="52"
        CanResize="True" ShowInTaskbar="False" Topmost="True"
        WindowDecorations="None" TransparencyLevelHint="Transparent"
        Background="Transparent" ExtendClientAreaToDecorationsHint="True">
  <Border Name="Shell" Background="Transparent">
    <Grid>
      <Border Name="PanelBackground" IsVisible="False" Background="#EE14161E"
              BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1" CornerRadius="18"
              BoxShadow="0 10 34 0 #73000000" />

      <Border Name="DockGlow" Width="48" Height="48" CornerRadius="24" HorizontalAlignment="Left"
              VerticalAlignment="Top" Margin="2" Classes="dockglow">
        <Border.Background>
          <RadialGradientBrush Center="50%,50%" GradientOrigin="50%,50%">
            <GradientStop Color="#CC8A63F5" Offset="0" />
            <GradientStop Color="#004F7CFF" Offset="1" />
          </RadialGradientBrush>
        </Border.Background>
      </Border>

      <Button Name="DockButton" Classes="accent" Width="44" Height="44" HorizontalAlignment="Left"
              VerticalAlignment="Top" Margin="3" CornerRadius="15" FontSize="19"
              ToolTip.Tip="Open Luma">&#x2726;</Button>

      <Grid Name="Panel" IsVisible="False" Opacity="0" RenderTransform="translateY(10px)"
            Margin="18,16,16,16" RowDefinitions="Auto,Auto,Auto,Auto,*,Auto,Auto">
        <Grid.Transitions>
          <Transitions>
            <DoubleTransition Property="Opacity" Duration="0:0:0.18" />
            <TransformOperationsTransition Property="RenderTransform" Duration="0:0:0.18" />
          </Transitions>
        </Grid.Transitions>
        <Grid Grid.Row="0" Name="HeaderBar" Background="Transparent"
              ColumnDefinitions="Auto,*,Auto,Auto,Auto,Auto" Margin="0,0,0,12">
          <Button Name="LogoButton" Classes="accent" Width="30" Height="30" CornerRadius="10"
                  Padding="0" FontSize="14" Content="&#x2726;" VerticalAlignment="Center" Margin="0,0,9,0"
                  ToolTip.Tip="Minimize to dock" />
          <StackPanel Name="DragHandle" Grid.Column="1" VerticalAlignment="Center" Spacing="1">
            <TextBlock Text="Luma" FontSize="15" FontWeight="SemiBold" />
            <TextBlock Text="{Binding LandingTitle}" FontSize="10.5" Opacity="0.55" />
          </StackPanel>
          <Border Grid.Column="2" Classes="statuspill" VerticalAlignment="Center" Margin="0,0,6,0">
            <StackPanel Orientation="Horizontal" Spacing="6">
              <Ellipse Width="8" Height="8" Fill="#8CB3A6FF" VerticalAlignment="Center" />
              <TextBlock Text="{Binding SurfaceStatus}" FontSize="10.5" />
            </StackPanel>
          </Border>
          <ComboBox Grid.Column="3" Width="86" FontSize="12" CornerRadius="10"
                    Background="#14FFFFFF" BorderBrush="#26FFFFFF"
                    SelectedIndex="{Binding SelectedProviderIndex}"
                    ItemsSource="{Binding Providers}" Margin="6,0" VerticalAlignment="Center" />
          <Button Grid.Column="4" Name="SettingsButton" Classes="ghost" Width="30" Height="30"
                  CornerRadius="10" Padding="0" VerticalAlignment="Center" Margin="0,0,2,0"
                  ToolTip.Tip="Settings">
            <Path Data="M4,8 L20,8 M4,16 L20,16 M14,8 m-2.4,0 a2.4,2.4 0 1 0 4.8,0 a2.4,2.4 0 1 0 -4.8,0 M9,16 m-2.4,0 a2.4,2.4 0 1 0 4.8,0 a2.4,2.4 0 1 0 -4.8,0"
                  Stroke="#99FFFFFF" StrokeThickness="1.7" StrokeLineCap="Round" Fill="#FF1B1D28"
                  Width="14" Height="14" Stretch="Uniform" />
          </Button>
          <Button Grid.Column="5" Name="ClosePanelButton" Classes="ghost" Content="&#x00D7;"
                  Width="30" Height="30" CornerRadius="10" FontSize="17" VerticalAlignment="Center"
                  ToolTip.Tip="Minimize to dock" />
        </Grid>

        <StackPanel Grid.Row="1" Spacing="6" Margin="0,0,0,8">
          <Border IsVisible="{Binding HasProviderProblem}" Background="#24FFB347" BorderBrush="#55FFB347"
                  BorderThickness="1" CornerRadius="9" Padding="9,6">
            <TextBlock Text="{Binding ProviderStatus}" FontSize="11" TextWrapping="Wrap" Foreground="#FFFFD39A" />
          </Border>
          <Border IsVisible="{Binding HasRunningOperations}" Background="#208A63F5" BorderBrush="#558A63F5"
                  BorderThickness="1" CornerRadius="9" Padding="9,6">
            <Grid ColumnDefinitions="*,Auto">
              <TextBlock Text="{Binding RunningStatus}" FontSize="11" Foreground="#D9C9BFFF" />
              <TextBlock Grid.Column="1" Text="Drag dock to STOP" FontSize="10" Opacity="0.6" />
            </Grid>
          </Border>
          <Border IsVisible="{Binding HasAssistantMemory}" Background="#18FFFFFF" BorderBrush="#2C8A63F5"
                  BorderThickness="1" CornerRadius="9" Padding="9,7">
            <StackPanel Spacing="3">
              <TextBlock Text="Pinned memory" FontSize="10.5" Foreground="#8CC9BFFF" />
              <TextBlock Text="{Binding AssistantMemoryPreview}" FontSize="11" Opacity="0.72" TextWrapping="Wrap" />
            </StackPanel>
          </Border>
        </StackPanel>

        <!-- Full image preview only for regions the user deliberately snipped; the ambient
             full-screen capture collapses to the slim pill below so it never dominates the chat. -->
        <Border Grid.Row="2" Name="PreviewBorder" IsVisible="{Binding HasRegion}" Height="150"
                CornerRadius="11" ClipToBounds="True" Background="#26000000"
                BorderBrush="#26FFFFFF" BorderThickness="1" Margin="0,0,0,10">
          <Grid>
            <Image Source="{Binding Preview}" Stretch="Uniform" />
            <Border Background="#A6000000" CornerRadius="7" Padding="7,2" Margin="6"
                    HorizontalAlignment="Left" VerticalAlignment="Bottom">
              <TextBlock Text="{Binding PreviewLabel}" FontSize="10" Foreground="#D9FFFFFF" />
            </Border>
            <Button Command="{Binding ClearCaptureCommand}" Classes="ghost" Content="&#x00D7;"
                    Width="26" Height="26" CornerRadius="9" FontSize="15"
                    Background="#59000000" ToolTip.Tip="Remove this capture"
                    HorizontalAlignment="Right" VerticalAlignment="Top" Margin="6" />
          </Grid>
        </Border>
        <Border Grid.Row="2" IsVisible="{Binding HasContextOnly}" Background="#10FFFFFF"
                BorderBrush="#1CFFFFFF" BorderThickness="1" CornerRadius="9"
                Padding="9,3" Margin="0,0,0,10">
          <Grid ColumnDefinitions="Auto,*,Auto">
            <Path Data="M4,5 L20,5 L20,15.5 L4,15.5 Z M9.5,19 L14.5,19 M12,15.5 L12,19"
                  Stroke="#8CB3A6FF" StrokeThickness="1.6" StrokeLineCap="Round" StrokeJoin="Round"
                  Width="13" Height="13" Stretch="Uniform" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Text="Luma can see your screen" FontSize="10.5" Opacity="0.55"
                       VerticalAlignment="Center" Margin="8,0,0,0" />
            <Button Grid.Column="2" Classes="ghost" Content="&#x00D7;" Width="20" Height="20"
                    CornerRadius="7" FontSize="13" Padding="0" Command="{Binding ClearCaptureCommand}"
                    ToolTip.Tip="Remove the screen context" />
          </Grid>
        </Border>

        <Grid Grid.Row="3" ColumnDefinitions="2*,*,Auto" ColumnSpacing="8" Margin="0,0,0,10">
          <Button Grid.Column="0" Classes="accent" Command="{Binding ExplainScreenCommand}"
                  IsEnabled="{Binding IsIdle}" HorizontalAlignment="Stretch" Height="38"
                  ToolTip.Tip="Capture and explain the current full screen">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="7">
              <Path Data="M4,5 L20,5 L20,15.5 L4,15.5 Z M9.5,19 L14.5,19 M12,15.5 L12,19"
                    Stroke="White" StrokeThickness="1.8" StrokeLineCap="Round" StrokeJoin="Round"
                    Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center" />
              <TextBlock Text="Explain this screen" FontSize="12.5" VerticalAlignment="Center" />
            </StackPanel>
          </Button>
          <Button Grid.Column="1" Classes="outline" Command="{Binding CaptureCommand}"
                  IsEnabled="{Binding IsIdle}" HorizontalAlignment="Stretch" Height="38">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8">
              <Path Data="M9,4 L6,4 C4.9,4 4,4.9 4,6 L4,9 M15,4 L18,4 C19.1,4 20,4.9 20,6 L20,9 M9,20 L6,20 C4.9,20 4,19.1 4,18 L4,15 M15,20 L18,20 C19.1,20 20,19.1 20,18 L20,15"
                    Stroke="#B3A6FF" StrokeThickness="2" StrokeLineCap="Round"
                    Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center" />
              <TextBlock Text="Snip a region" FontSize="12.5" VerticalAlignment="Center" />
            </StackPanel>
          </Button>
          <Button Grid.Column="2" Classes="ghost" Command="{Binding ExplainSelectionCommand}"
                  IsEnabled="{Binding IsIdle}" Width="38" Height="38" CornerRadius="12"
                  ToolTip.Tip="Select an area and explain it">
            <TextBlock Text="&#x2726;" FontSize="15" VerticalAlignment="Center" />
          </Button>
        </Grid>

        <Grid Grid.Row="4" Margin="0,0,0,10">
          <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="10"
                      IsVisible="{Binding !Messages.Count}">
            <TextBlock Text="{Binding LandingTitle}"
                       FontSize="12" Opacity="0.45" TextAlignment="Center" TextWrapping="Wrap"
                       MaxWidth="260" />
            <TextBlock Text="{Binding LandingSubtitle}"
                       FontSize="11" Opacity="0.38" TextAlignment="Center" TextWrapping="Wrap"
                       MaxWidth="270" />
            <TextBlock Text="Ctrl+Shift+E selects and explains from any app" FontSize="10.5" Opacity="0.45"
                       HorizontalAlignment="Center" IsVisible="{Binding GlobalExplainShortcutAvailable}" />
            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center"
                        IsVisible="{Binding IsSuggesting}" Margin="0,6,0,0">
              <Ellipse Classes="thinkingring" />
              <TextBlock Text="{Binding SurfaceDetail}" FontSize="11.5" Opacity="0.5"
                         VerticalAlignment="Center" />
            </StackPanel>
            <ItemsControl ItemsSource="{Binding Suggestions}" HorizontalAlignment="Center"
                          MaxWidth="420" Margin="0,6,0,0">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                  <StackPanel Spacing="8" />
                </ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Button Classes="chip" Content="{Binding}" HorizontalAlignment="Center"
                          Click="OnSuggestionClick" ToolTip.Tip="Use this shortcut" />
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </StackPanel>
          <ScrollViewer Name="ChatScroll" VerticalScrollBarVisibility="Auto"
                        IsVisible="{Binding Messages.Count}">
            <ItemsControl ItemsSource="{Binding Messages}">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Border Classes="bubble" Classes.user="{Binding IsUser}" Classes.error="{Binding IsError}"
                          Classes.codecard="{Binding HasCodeSession}">
                    <Grid>
                      <StackPanel Spacing="5">
                        <StackPanel Orientation="Horizontal" Spacing="7"
                                    IsVisible="{Binding Caption, Converter={x:Static ObjectConverters.IsNotNull}}">
                          <TextBlock Text="{Binding Caption}" FontSize="10.5" Foreground="#8CC9BFFF"
                                     VerticalAlignment="Center" />
                          <Ellipse Classes="thinkingring" IsVisible="{Binding IsPending}" />
                          <TextBlock Text="{Binding Elapsed}" FontSize="10.5" Foreground="#59FFFFFF"
                                     VerticalAlignment="Center" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="7" IsVisible="{Binding IsPending}">
                          <Ellipse Classes="thinkingring" Width="12" Height="12" />
                          <TextBlock Text="{Binding Text}" FontSize="12.5" FontStyle="Italic"
                                     Opacity="0.72" TextWrapping="Wrap" />
                        </StackPanel>
                        <c:MarkdownView Markdown="{Binding Text}" IsVisible="{Binding !IsPending}" />
                        <TextBlock Text="|" IsVisible="{Binding IsStreaming}" Classes="streamcursor"
                                   Foreground="#B3A6FF" FontSize="13" />
                        <Border IsVisible="{Binding IsQuestion}" Classes="questionhint" CornerRadius="9"
                                Padding="9,6" Margin="0,4,0,0">
                          <TextBlock Text="The assistant asked a question - check the popup" FontSize="11.5" />
                        </Border>
                        <c:DiffCardControl Session="{Binding CodeSession}" IsVisible="{Binding HasCodeSession}"
                                           Margin="0,6,0,0" />
                      </StackPanel>
                      <Button Classes="msgcopy" Content="copy" HorizontalAlignment="Right" VerticalAlignment="Top"
                              IsVisible="{Binding !IsUser}" ToolTip.Tip="Copy answer"
                              Command="{Binding $parent[ItemsControl].DataContext.CopyMessageCommand}"
                              CommandParameter="{Binding}" />
                    </Grid>
                  </Border>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </ScrollViewer>
        </Grid>

        <StackPanel Grid.Row="5" Margin="0,0,0,8" Spacing="6">
          <TextBlock Text="Next steps" FontSize="10.5" Opacity="0.5"
                     IsVisible="{Binding Messages.Count}" />
          <ItemsControl ItemsSource="{Binding Suggestions}" IsVisible="{Binding Messages.Count}">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate><WrapPanel Orientation="Horizontal" /></ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Button Classes="chip" Content="{Binding}" Click="OnSuggestionClick"
                        Margin="0,0,6,6" ToolTip.Tip="Send this reply" />
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
          <StackPanel Orientation="Horizontal" Spacing="7" IsVisible="{Binding IsSuggesting}">
            <Ellipse Classes="thinkingring" Width="12" Height="12" />
            <TextBlock Text="Preparing replies..." FontSize="10.5" Opacity="0.5" />
          </StackPanel>
        </StackPanel>

        <Grid Grid.Row="6" ColumnDefinitions="*,Auto" ColumnSpacing="8">
          <TextBox Name="QuestionBox" Text="{Binding Question}" AcceptsReturn="True"
                   PlaceholderText="Ask about your screen, or anything else... (Enter to send)" CornerRadius="12"
                   MinHeight="42" MaxHeight="120" TextWrapping="Wrap" FontSize="13"
                   Background="#14FFFFFF" BorderBrush="#26FFFFFF" />
          <Grid Grid.Column="1" VerticalAlignment="Bottom">
            <Button Classes="accent" Command="{Binding SendCommand}" IsEnabled="{Binding CanSend}"
                    IsVisible="{Binding !IsBusy}" Width="42" Height="42" CornerRadius="14"
                    Padding="0" ToolTip.Tip="Send">
              <Path Data="M4,12 L19,12 M12.5,5.5 L19,12 L12.5,18.5" Stroke="White" StrokeThickness="2"
                    StrokeLineCap="Round" StrokeJoin="Round" Width="15" Height="15" Stretch="Uniform" />
            </Button>
            <Button Classes="stop" Command="{Binding StopCommand}" IsVisible="{Binding IsBusy}"
                    Width="42" Height="42" CornerRadius="14" Padding="0" ToolTip.Tip="Stop">
              <Rectangle Width="11" Height="11" RadiusX="2.5" RadiusY="2.5" Fill="#FFFF9AA6" />
            </Button>
          </Grid>
        </Grid>
      </Grid>

      <Border Name="ResizeGrip" Width="18" Height="18" Background="Transparent" IsVisible="False"
              HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,5,5"
              Cursor="BottomRightCorner" ToolTip.Tip="Drag to resize">
        <Path Data="M12,4 L4,12 M12,8 L8,12" Stroke="#59FFFFFF" StrokeThickness="1.5"
              StrokeLineCap="Round" />
      </Border>
    </Grid>
  </Border>
</Window>
